<div class="flex justify-evenly absolute bottom-0 w-full py-4 border-t">
    <a href="{{ route('home', absolute: false) }}" class="border">Home</a>
    <a href="{{ route('recipes.index', absolute: false) }}" class="border">Recipe</a>
    <a href="{{ route('ingredients.index', absolute: false) }}" class="border">Stock</a>
    <a href="{{ route('shops.index', absolute: false) }}" class="border">Store</a>
</div>
